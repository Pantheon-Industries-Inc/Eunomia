# `contracts/release/` — the release metadata

**Filled in Run 0b.** The per-episode record Eunomia emits for the **Hermes** analytical platform to
ingest — the join of sidecar (§2) + operational model (§3) resolved as-of `recorded_at`, frozen at
ingest, plus the derived QC / sync / pairing fields. **This is the platform-input contract Hermes
pins a version of** (the anti-drift discipline).

Authoritative definition: `docs/CONTRACT.md` §4.
