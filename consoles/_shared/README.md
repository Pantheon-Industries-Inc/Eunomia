# `consoles/_shared/`

**Filled in its own run.** Common components + the contract-typed client shared across the consoles so
the apps don't reinvent it — while the apps remain separately deployable islands. (Web stack choice is
deferred — MODULE_MAP open-Q 1.)
