# `consoles/gods-view/`

**Filled in its own run.** The live ops dashboard (consumes `contracts/events/`). **Exception-first**
— healthy operators stay quiet, problems surface — NOT live video (which doesn't scale to ~32 wrist
cameras per lead). Shows recording/connected/flagged-rate/battery/SD + the help-call inbox. More
real-time than the other consoles (may use a different stack — MODULE_MAP open-Q 1).
