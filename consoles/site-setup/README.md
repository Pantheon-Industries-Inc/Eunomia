# `consoles/site-setup/`

**Filled in its own run.** HQ console: site WiFi + telemetry endpoint + the task-menu (the config the
fob pulls). Also the likely home of the start-of-day capture-stack confirm (B-9).
