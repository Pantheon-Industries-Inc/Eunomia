# `ingest/release/`

**Filled in its own run.** Assembles + emits the release record (the `contracts/release/` shape) for
Hermes; resolves the operational model as-of the recording time; freezes the join. This is the clean
handoff: "here is the labeled, QC'd, paired episode and where its bytes are." (CONTRACT §4.)
